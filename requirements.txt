# O PORQUE: Streamlit fixado numa versão exata confirmada (evita que uma
# atualização automática mude o comportamento do app sem você perceber,
# especialmente no Community Cloud, que reinstala tudo a cada deploy). As
# demais dependências usam um piso mínimo (>=) em vez de uma versão exata,
# porque eu não tinha como confirmar ao certo qual é a última versão de
# cada uma no momento em que gerei este arquivo -- prefiro isso a arriscar
# fixar um número que nem exista e quebrar a instalação. Se quiser fixar
# todas depois, rode `pip freeze` no ambiente já funcionando e copie os
# números exatos para cá.
streamlit==1.59.0
pandas>=2.2
numpy>=1.26
plotly>=5.24
libsql>=0.1.0
# O PORQUE: usado para gerar/checar hashes de senha em [credentials] no
# secrets.toml (bcrypt.hashpw / bcrypt.checkpw), em vez de guardar a senha
# em texto puro -- ver gerar_hash_senha.py.
bcrypt>=4.1
# O PORQUE: já vinha instalado como dependência transitiva do Streamlit,
# mas o app agora chama requests.post() diretamente (pra falar com o
# webhook de estimativa de esforço por IA no n8n) -- declarar aqui deixa
# essa dependência explícita, em vez de confiar que uma versão futura do
# Streamlit continue trazendo requests "de brinde".
requests>=2.27
# O PORQUE: reportlab monta o PDF em si (texto + imagens). Para os
# gráficos dentro do PDF, uso matplotlib (não o Plotly diretamente) --
# Plotly precisaria do kaleido para virar imagem estática, e o kaleido tem
# um bug antigo e nunca resolvido (issues abertas desde 2020) de travar
# indefinidamente no Windows sem erro nenhum. matplotlib é Python puro,
# sem subprocesso/navegador nenhum por trás -- não tem essa categoria de
# problema, e já testei rodando de ponta a ponta antes de entregar.
reportlab>=4.0
matplotlib>=3.8
